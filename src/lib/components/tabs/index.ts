import { default as Root } from "./tabs-root.svelte"
import { default as List } from "./tabs-list.svelte"
import { default as Tab } from "./tabs-tab.svelte"
import { default as Content } from "./tabs-content.svelte"

export const Tabs = {
    Root,
    List,
    Tab,
    Content,
}

export default Tabs
